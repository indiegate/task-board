import * as BoardReducer from '../../reducers/boardReducer';

describe('BoardReducer', () => {
  xit('should init layout fetching', () => {});
  xit('should process layout', () => {});
  xit('should add task', () => {});
  xit('should cancel task adding', () => {});
});
